mixin header-link(name, href)
    li.back-animated-scale.topbar-li
        a(href="#" + href)=name

doctype html
html(lang='fr')
    head
        meta(charset='UTF-8')
        title Maxime Moreau - Développeur web
        meta(name='viewport', content='width=device-width, initial-scale=1.0, maximum-scale=1.0')

        meta(http-equiv="content-language" content="fr")
        meta(name="language" content="fr")

        meta(name="description" value="Maxime moreau jeune développeur web dans les environs de Calais, passionné par la programmation, étudiant en DUT Informatique.")
        script(type="application/ld+json").
            {
              "@context": "http://schema.org",
              "@type": "Organization",
              "name" : "Maxime Moreau"
              "url": "maxime-moreau.fr",
              "sameAs": [
                "https://twitter.com/emixdev"
              ]
              "contactPoint": [{
                "@type": "ContactPoint",
                "telephone": "06-19-01-59-88",
                "email": "mailto:contact@maxime-moreau.fr"
              }]
            }
        script.
          (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
          (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
          m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
          })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

          ga('create', 'UA-86717305-1', 'auto');
          ga('send', 'pageview');
    
    include svg/svg
    body
        .fake#fake
        .topbar#topbar
          a#topbar-trigger.topbar-trigger(href="#")
          .topbar-left
            a.topbar-title(href='#home') Maxime
            nav.topbar-menu#topbar-menu
              ul.topbar-ul
                +header-link('Accueil', 'home')
                +header-link('Présentation', 'presentation')
                +header-link('Compétences', 'skills')
                +header-link('Créations', 'creations')
                +header-link('Contact', 'contact')
        //- header.header#header
        //-     a#header__icon.header__icon(href='#')
        //-     a.header__title(href='#') Maxime
        //-     // Pour le name
        //-     nav.header__nav
        //-       ul.header__nav__ul
        //-         +header-link('Accueil', 'home')
        //-         +header-link('Présentation', 'presentation')
        //-         +header-link('Compétences', 'skills')
        //-         +header-link('Créations', 'creations')
        //-         +header-link('Contact', 'contact')

        .scroll-resp
            .site-cache
            block content
            include ./footer
    block script
