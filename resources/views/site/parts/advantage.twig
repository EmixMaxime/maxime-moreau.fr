{% macro chartItem (title, percentage) %}
  <div class="chart-item">
    <div class="chart-item-baton" data-percentage={{ percentage }}>
      <span class="chart-item-text">{{ title }}</span>
    </div>
  </div>
{% endmacro %}

{% from _self import chartItem %}

<section class="l-section" id="advantages-section">
  <div class="l-container">
    <h1 class="h2 text-center">{{ "Mes atouts" | trans }}</h1>

    <div class="chart" id="chart">
    
      <div class="chart-legend chart-legend--left">
        <div class="chart-legend-title">
          <span>{{ "moi" | trans }}</span>
        </div>
      </div>
      
      <div class="chart-legend chart-legend--right">
        <ul>
          <li class="chart-legend-item">Linus Torvlads</li>
          <li class="chart-legend-item">{{ "Très bon" | trans }}</li>
          <li class="chart-legend-item">{{ "Bon" | trans }}</li>
        </ul>
      </div>


      <div class="chart-items">
        {{ chartItem('Curieux' | trans, 88) }}
        {{ chartItem('Sérieux' | trans, 78) }}
        {{ chartItem('Ambitieux' | trans, 69) }}
        {{ chartItem('Leadership', 58) }}
      </div>

    </div>
  </div>
</section>